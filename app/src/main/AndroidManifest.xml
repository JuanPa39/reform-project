<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StationAdviser"
        tools:targetApi="31">

        <!-- LOGIN -->
        <activity
            android:name=".Activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ACTIVIDADES PRINCIPALES -->
        <activity android:name=".Activities.MainActivity" android:exported="false"/>
        <activity android:name=".Activities.RoleBaseActivity" android:exported="false"/>
        <activity android:name=".Activities.RegisterActivity" android:exported="false"/>
        <activity android:name=".Activities.ResetPasswordActivity" android:exported="false"/>

        <!-- ESTACIONES -->
        <activity android:name=".Activities.RegistrarEstacionActivity" android:exported="false"/>
        <activity android:name=".Activities.ListaEstacionesActivity" android:exported="false"/>

        <!-- COMBUSTIBLE -->
        <activity android:name=".Activities.RegistrarPrecioCombustibleActivity" android:exported="false"/>
        <activity android:name=".Activities.ConsultarPrecioActivity" android:exported="false"/>

        <!-- NUEVOS CASOS DE USO -->
        <activity android:name=".Activities.RegistrarInventarioActivity" android:exported="false"/>
        <activity android:name=".Activities.RegistrarVentaActivity" android:exported="false"/>
        <activity android:name=".Activities.HistorialVentasActivity" android:exported="false"/>
        <activity android:name=".Activities.ReporteMensualActivity" android:exported="false"/>

    </application>

</manifest>